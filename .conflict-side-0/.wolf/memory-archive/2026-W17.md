# 2026-W17 — Week of Apr 20–26 (ADR explosion + style audits + Phase 1 G1-G7)

**Week range:** 2026-04-20 (Mon) → 2026-04-26 (Sun) (ISO 8601 W17)
**Commits:** 341 (PRs #56 → ~#180)
**Sessions logged in `.wolf/memory.md`:** Apr 20–26 (full week)
**Authoritative sources:** `git log --since=2026-04-20 --until=2026-04-27`; ADRs 0019–0049; `icm/_state/active-workstreams.md` history

---

## Major decisions

- **ADRs 0019–0049 drafted** (~30 ADRs in one week). Largest ADR-shipping week in repo history. Covers: reporting pipeline (0021), rich-vs-MVP component tier (0022), dialog slots / button variants / CSS prefix (0023–0025), 5-ADR en-bloc accept (0026–0030: undocumented bundle), Bridge Zone-C SaaS (0031), multi-team Anchor (0032), browser shell v1 (0033), a11y-harness-per-adapter (0034), global-domain-types (0035), syncstate contract (0036), CI platform (0037), backfill 0038–0042 (rulesets / checks / i18n / dual-namespace / subagent dispatch), unified threat model (0043), MAUI multi-backend (0044), key-loss recovery (0046), audit substrate (0049), MAUI cross-OS extension (0048).
- **Council-review pattern formalized** (PR #157). Council review of ADRs 0037–0042 (6 ADRs, 30 seat-verdicts). Establishes the multi-perspective adversarial review pattern that becomes Stage 1.5 in the universal-planning rubric and the standard pre-acceptance step for substantive ADRs.
- **Bridge Zone-C SaaS shape decided** (ADR 0031 + 0032). Bridge = hosted-node-as-SaaS with per-tenant data-plane isolation; Anchor = local-first desktop with multi-team workspace switching (Slack-style). These define the canonical accelerator zones referenced by every later cluster ADR.
- **Component tier policy: Rich vs MVP** (ADR 0022). Resolves the dual-component-namespace question (SunfishGantt/Scheduler/Spreadsheet/PdfViewer in two folders by design — never deduplicate without an api-change pipeline). Anchors a memory note for sunfish-PM session.
- **Accelerator / iOS / kernel-security expansion** (Phase 1 G1–G7 series). G1 i18n locale-coordinators-roster + i18n-platform; G2 CRDT-engine + Anchor wiring; G3 dual SaaS+Relay AppHost smoke profile; G4 dual-discovery; G5 backup-orchestration export/import primitives; G6 paper-key recovery (BIP-39 — #48c) + trustee recovery records (#48a/#48f). Establishes Phase 1 MVP scaffolding for the local-node architecture.
- **Style-audit phase shipped** (~30 audit commits across `audit(spec-verify)`, `audit(a11y)`, `audit(i18n)`, `style-audits/TIER-*`). 248 findings across 4 components × 3 frameworks; 10 systemic themes; 3-phase remediation begun. Synthesis at `icm/07_review/output/style-audits/SYNTHESIS.md`.
- **i18n / localization shipping** (~22 commits). i18n locales.json + coordinators roster + ADR 0036 syncstate contract. `_Locales.json` files per package; en/es/fr base coverage. Sets the localization pattern repo-wide.
- **CSS prefix sweep** (`mar-*` → `sf-*`, ADR 0025; PR around #100). 355 renames; BEM convention adopted. Anchored by an analyzer to prevent regression.
- **Public listing flag flipped**: repo became PUBLIC during this week (was private; later flipped back). The threat model in ADR 0043 was driven by this transition. Memory note `project_sunfish_private_until_llc` reflects current state (public again).
- **Flease ledger wiring for CP-class writes** (D6, PR #155). Kernel-ledger + scheduling adopt Flease for CP-class write coordination. First production use of the per-record-class CP/AP positioning from the paper.

## Non-obvious outcomes

- **EFCore + Npgsql preview lock** (memory `reference_efcore_npgsql_preview_lock`). Discovered: EFCore preview upgrades are gated on a matching Npgsql preview; Aspire-tracked packages follow Aspire cadence not runtime cadence. Triggered by a routine dep bump that had to be reverted.
- **`commitlint` type-enum strict enforcement** (memory `project_commitlint_type_enum`). Discovered: `security` is NOT in the allowed type-enum; must use `ci(security)` / `chore(security)` / `docs(security)`. Surfaced after a hook block on a security-themed commit.
- **GH Actions schema rejects `paths:` + `paths-ignore:` simultaneously** (memory `reference_github_actions_paths_schema`). Discovered: combining them is a workflow-loading error that breaks push events globally. Fix: pick one mode per trigger.
- **Spec-verify audit found ICM ledger drift in 6 ADRs** (PRs #142, #140). Broken xrefs in `apps/docs/foundation` to ADRs 0007/0008/0009/0012/0013/0015 + 0021. Root cause: ADR moves earlier in week didn't update doc xrefs. Fix: explicit cross-ref check added to docs CI.
- **WCAG 2.2 AA deep audit on scheduler family** (PR #159). Critical + Serious fixes shipped; Moderate tracked. Established the a11y-deep-audit cadence.
- **Pre-release latest-first policy adopted** (memory `project_pre_release_latest_first_policy`). Pre-LLC: track latest preview/RC of every package; no LTS floor. Inverts at first public release. Reduces dependabot triage burden mid-project.
- **The package-icon SVG-render bug** (between #56 and #80; commit "icons render as SVG, not HTML-escaped text"). Icons were rendering as text strings; HTML escaping was wrong. Fix: switch to dangerouslySetInnerHTML equivalent in adapter; tested via kitchen-sink visual diff. Logged in `.wolf/buglog.json`.
- **Sleep-on-MCP-quota-exhaustion pattern** (memory `feedback_sleep_on_quota_exhaustion`). Established mid-week: pause new agent dispatches that need Context7 / MCP docs; ScheduleWakeup 3600s and re-probe. In-flight agents may proceed.
- **Sleep-on-Claude-Code-token-exhaustion pattern** (memory `feedback_sleep_on_claude_code_token_exhaustion`). Established: on Claude Code usage-limit / rate-limit reset windows, stop all dispatch; ScheduleWakeup past reset and re-probe.

## State at week-end (Sun 2026-04-26)

- **49 ADRs** Accepted or in-flight (0001–0049). 0050 (deferred-multi-tenant) drafted but deferred.
- **Phase 1 MVP G1–G7** scaffolds shipping: i18n + CRDT + dual discovery + backup orchestration + key-loss recovery (paper-key + trustee + soon-to-add social-recovery 48a)
- **Style-audit synthesis** at 86.5% Phase 1 (45/52 P0 resolved; 7 remaining)
- **Bridge profiles**: Phase1Smoke launches dual SaaS+Relay AppHost cleanly
- **Anchor**: CRDT engine wired in MauiProgram; multi-team workspace switching ADR-Accepted (ADR 0032)
- **Memory + cerebrum**: project memories under `~/.claude/projects/-Users-christopherwood-Projects-Sunfish/memory/` populated with ~10 cross-session pointers

## Pointer to authoritative sources

- `git log --since="2026-04-20" --until="2026-04-27" --pretty=format:"%h %ad %s"` — full 341-commit detail
- `docs/adrs/0019-*.md` through `docs/adrs/0049-*.md` — formal decision records as drafted in this week
- `icm/07_review/output/style-audits/SYNTHESIS.md` — style-audit findings synthesis
- `icm/07_review/output/style-audits/TIER-4-RE-AUDIT.md` — Phase 1 P0 status (45/52 resolved by week-end)
- `~/.claude/projects/-Users-christopherwood-Projects-Sunfish/memory/MEMORY.md` — cross-session memory index built up during this week
