// NOTE: The ColorPickerFormat enum is declared in
// Sunfish.Foundation.Enums (packages/foundation/Enums/ColorPickerEnums.cs).
// This file exists only to keep the enum surface discoverable under the
// adapters' `Enums/` folder for the ICM stage's `git add` pattern. Adding a
// duplicate declaration here would cause CS0101. See CalendarView.cs for the
// same convention.
//
// Consumers should import ColorPickerFormat via `Sunfish.Foundation.Enums`
// (already wired through ui-adapters-blazor/_Imports.razor).

// intentionally empty
