@* Pass-through wrapper for spec-compatible markup structure.
   Consumers can nest SunfishSplitterPane children inside this wrapper
   or place them directly inside SunfishSplitter (both patterns work). *@

@ChildContent

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
