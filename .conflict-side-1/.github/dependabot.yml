version: 2
updates:
  # NuGet — Central Package Management (Directory.Packages.props)
  - package-ecosystem: nuget
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    # Match repo's commitlint type-enum: feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert
    # Per memory project_commitlint_type_enum + commitlint.config.mjs.
    commit-message:
      prefix: "chore(deps)"
      include: "scope"
    groups:
      aspnetcore:
        patterns:
          - "Microsoft.AspNetCore.*"
          - "Microsoft.AspNetCore.SignalR.*"
      # EFCore + Npgsql adapter must bump together: Npgsql.EntityFrameworkCore.PostgreSQL
      # transitively requires Microsoft.EntityFrameworkCore.Relational at the matching
      # preview, so partial bumps trigger NU1605 (package downgrade detected). See
      # memory reference_efcore_npgsql_preview_lock.
      efcore:
        patterns:
          - "Microsoft.EntityFrameworkCore"
          - "Microsoft.EntityFrameworkCore.*"
          - "Npgsql.EntityFrameworkCore.PostgreSQL"
      testing:
        patterns:
          - "xunit*"
          - "bunit*"
          - "Microsoft.NET.Test.Sdk"
          - "coverlet.*"

  # GitHub Actions — pin to latest SHAs
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: "chore(deps)"
      include: "scope"
