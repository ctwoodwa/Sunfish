---
type: question
workstream-or-chapter: W#45 P2
last-pr: (held; not pushed — local branch feat/w45-p2-blocks-crew-comms)
resolved-by: ADR 0076-A1 PR #548 (stacked on #534)
resolved-date: 2026-05-04
---

W#45 P2 (`blocks-crew-comms` + EncryptionHandshake) built locally; council pre-merge returned **RETURN-FOR-REWORK** on canonical-form questions that need ADR 0076-A1 amendment. PR push held; 13/13 tests pass; mechanical findings already applied.

**What needed XO authoring (ADR 0076-A1):**

1. **Transcript-hash canonical form** — Ratified: reduced form
   SHA-256(ephemA||idA||ephemB||idB||uint32BE(len(tenantBytes))||tenantBytes||negotiatedCap)
   — robust to MessagePack-key-ordering; full-frame-bytes are brittle; impl already matched (b).

2. **HELLO + HEARTBEAT signables / tenantId** — Ratified: UTF-8 bytes of TenantId.Value.
   TenantId is string-backed; no UUID semantics; avoiding UUID-backed variant avoids touching
   every multi-tenant package.

3. **HEARTBEAT peerId field** — Ratified: raw bytes[32] Ed25519 identity public key
   (PrincipalId bytes via PeerId.From(PrincipalId)). UUID constraint scoped to messageId only.

**Resolution:** PR #548 (docs/adr-0076-a1-wire-encoding) stacked on PR #534.
Once #534 merges, retarget #548 to main and enable auto-merge. COB then rebases
held branch and pushes W#45 P2 PR.
