---
type: question
workstream-or-chapter: W#45 P2
last-pr: (held; not pushed — local branch feat/w45-p2-blocks-crew-comms)
---

W#45 P2 (`blocks-crew-comms` + EncryptionHandshake) built locally; council pre-merge returned **RETURN-FOR-REWORK** on canonical-form questions that need ADR 0076-A1 amendment. PR push held; 13/13 tests pass; mechanical findings already applied.

**What needs XO authoring (ADR 0076-A1):**

1. **Transcript-hash canonical form** — Pick (a) full MessagePack-serialized HELLO/INVITE/ACCEPT bytes per ADR §step 9 as written, OR (b) reduced form `(ephemA || idA || ephemB || idB || tenantBytes || negotiatedCap)`. Council recommends (b) — the reduced form is robust to MessagePack-key-ordering; full-frame-bytes are brittle. Current impl matches (b).

2. **HELLO + HEARTBEAT signables** — ADR §wire protocol claims `tenantId: uuid` (16 bytes RFC 4122) but `Sunfish.Foundation.Assets.Common.TenantId(string Value)` is string-backed (no UUID semantics). Hand-off line 207 inherited the same claim. Either redefine TenantId on the wire as UTF-8 bytes of `TenantId.Value` (current impl) or introduce a parallel UUID-backed TenantId variant (heavier — touches every multi-tenant package).

3. **HEARTBEAT peerId field** — ADR claims `peerId: uuid`; impl signs over raw 32-byte Ed25519 identity public key (PrincipalId bytes). Current impl is internally consistent (peerId == base64url of 32-byte pubkey by `PeerId.From(PrincipalId)` definition). A1 should drop `uuid` claim and replace with raw-32-byte-pubkey-or-base64url-string.

**Full council report**: 1 Critical + 4 Major + 10 Minor + 2 Encouraged. Mechanical findings (Presence-heartbeat verification, ConfirmMismatchRejects test, SessionKey ownership doc, AEAD round-trip test, KeyPair.Sign XML doc) already applied to local commit.

**What unblocks me:** ADR 0076-A1 ratifying choices for items 1+2+3 above. Once A1 lands on main, I rebase the held branch + push + auto-merge. Estimated XO author work: ~1–2h (small textual amendment; no structural changes).
