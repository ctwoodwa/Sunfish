---
sort_order: 39
number: 36
slug: bridge-anchor-subscription-event-emitter
title: "Bridge → Anchor subscription-event-emitter (ADR 0031-A1+A1.12 contract surface)"
status: "built"
status_cell: "`built` (5 phases shipped 2026-05-01)"
owner: "sunfish-PM"
owner_cell: "sunfish-PM ✓"
reference_cell: "https://github.com/ctwoodwa/Sunfish/pull/451 (P1 — scaffold + 10 AuditEventType) + https://github.com/ctwoodwa/Sunfish/pull/453 (P2 — HMAC + idempotency + replay window + audit factory) + https://github.com/ctwoodwa/Sunfish/pull/455 (P3 — webhook delivery + retry + dead-letter + trust pinning) + https://github.com/ctwoodwa/Sunfish/pull/456 (P4 — SSE + webhook registration + secret rotation) + P5 (this PR — Anchor handler + DI + apps/docs + ledger flip)"
---

## Notes

**Built 2026-05-01 across 5 phases.** P1: `Sunfish.Bridge.Subscription` package scaffold + `BridgeSubscriptionEvent` record per A1.2 + 4 enums (`BridgeSubscriptionEventType` 7 values per A1.1 / `DeliveryMode` / `SignatureAlgorithm` Ed25519 reserved per A1.12.2 / `WebhookRegistration` + `SubscribedEventFilter` records per A1.4) + 10 new `AuditEventType` constants in kernel-audit (7 Bridge-side + 3 Anchor-side); canonical-JSON wire format mirrors W#34/W#35/W#30 cohort patterns. P2: `IEventSigner` + `HmacSha256EventSigner` (HMAC-SHA256 over canonical-JSON with signature field zeroed; `FixedTimeEquals`; 5 rejection paths) + `IIdempotencyCache` + `InMemoryIdempotencyCache` (per-tenant LRU; 24h retention) + `ReplayWindow` (±5min default, tunable; boundary-inclusive) + `BridgeSubscriptionAuditPayloads` factory (6 entry points covering 10 W#36 audit events, alphabetized). P3: `WebhookRetryPolicy` (canonical 7-attempt schedule 1s/5s/30s/5min/30min/2h/12h per A1.5) + `IDeadLetterQueue` + `InMemoryDeadLetterQueue` + `IWebhookDeliveryService` + `DefaultWebhookDeliveryService` (HTTPS POST + 30s timeout + 8-attempt retry loop + DLQ on terminal failure; injectable delay sink for fast tests; `DeliveryAttempt` field bumps 1-indexed) + `ITrustChainResolver` + `DefaultTrustChainResolver` (defaults to `WebhookTrustMode.PubliclyRootedCa` per A1.12.3 — halt-condition #4 honored; `ConfigurePinnedCertificate` PEM upload + `AllowSelfSigned` admin opt-in overrides). P4: `SseReconnectPolicy` (UNBOUNDED 1s/5s/30s/60s-capped per A1.12.4 — distinct type from `WebhookRetryPolicy` per halt-condition #2) + `SseQueueOverflowPolicy` (1h max-age + 10000-event max-depth thresholds per A1.12.4) + `IWebhookRegistrationService` + `DefaultWebhookRegistrationService` (HTTPS-only + non-loopback validation per A1.4; 32-byte auto-generated shared secret) + `ISharedSecretStore` + `InMemorySharedSecretStore` (90-day rotation cadence per A1.12.1; 24h grace window during which both old + new secrets verify). P5: `IBridgeSubscriptionEventHandler` + `InMemoryBridgeSubscriptionEventHandler` (Anchor-side 6-step protocol per A1.6: HMAC verify with current OR previous-in-grace per A1.12.1 → replay window → idempotency → IEditionCacheUpdater callback → audit emission → HandlerResponseStatus.Ok/SignatureFailed/Stale/AlreadyProcessed) + `AddInMemoryBridgeSubscription()` + `AddInMemoryBridgeSubscription(TenantId)` DI extensions (W#32 both-or-neither at the registration layer) + `apps/docs/bridge/subscription-events/overview.md` walkthrough + `apps/docs/toc.yml` Bridge entry + this ledger flip. **Closes ADR 0062-A1.6 halt-condition** (sub-second `EditionCapabilities` responsiveness path is end-to-end functional once Anchor wires `IEditionCacheUpdater`). All 4 named halt-conditions stayed off the trip-wire (HMAC vs Ed25519 confusion; SSE-vs-webhook retry conflation; secret persistence shape; trust default false). 4 secondary halt-conditions named in hand-off were either covered (CP-record quorum integration N/A — handler is event-driven not consensus-bound; AddInMemoryX two-overload pattern adopted; 30-second timeout for slow links per A1.3) or deferred (full WireGuard data-plane connect timing — host responsibility per A1.3). Substrate-only; consumer wiring (ADR 0062 Phase 1's `EditionCapabilities` integration via `IEditionCacheUpdater`) is separate workstream when COB capacity opens. Mirrors W#34/W#35/W#30 cohort patterns: ConcurrentDictionary state; two-overload constructor both-or-neither; `JsonStringEnumConverter` for all enums; `AddInMemoryX()` DI extension; `apps/docs/X/overview.md` page convention; alphabetized audit-payload keys; NSubstitute test-double pattern.
