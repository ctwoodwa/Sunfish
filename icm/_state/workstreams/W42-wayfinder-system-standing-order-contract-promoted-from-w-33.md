---
sort_order: 45
number: 42
slug: wayfinder-system-standing-order-contract-promoted-from-w-33
title: "**Wayfinder System + Standing Order Contract** (ADR 0065; `sunfish-feature-change` pipeline) — promoted from W#33-cohort follow-on per CO 2026-05-01"
status: "built"
status_cell: "`built` (P1+P2+P2-amend+P3a+P3b+P4 substrate shipped 2026-05-04; P3b perf-tests + Phase-4 kitchen-sink wiring + architecture-principles.md cross-link deferred per cohort norm)"
owner: "sunfish-PM"
owner_cell: "sunfish-PM ✓"
reference_cell: "https://github.com/ctwoodwa/Sunfish/pull/503 (P1) + https://github.com/ctwoodwa/Sunfish/pull/504 (P2) + https://github.com/ctwoodwa/Sunfish/pull/505 (P2 amendments) + https://github.com/ctwoodwa/Sunfish/pull/510 (P3a Atlas) + https://github.com/ctwoodwa/Sunfish/pull/513 (P3b analyzer) + https://github.com/ctwoodwa/Sunfish/pull/514 (P4 docs + WCAG baseline)"
---

## Notes

**Built 2026-05-04 across 6 PRs in a single sunfish-PM session.** P1 substrate types (#503): 14 types + 5 new `AuditEventType` constants (`StandingOrderIssued` / `StandingOrderAmended` / `StandingOrderRescinded` / `StandingOrderRejected` / `StandingOrderConflictResolved`); 24 tests. P2 (#504): `CrdtStandingOrderRepository` + `DefaultStandingOrderIssuer` reference impl with audit-by-construction at type level (`IAuditTrail` parameter required); 13 tests. P2 amendments (#505): council A-1 (`Accepted` field defensiveness) + A-2 (silent-break → loud `InvalidOperationException`) + A-3 (idempotency doc soften). P3a Atlas projector (#510): `IAtlasProjector` + `AtlasView` + `AtlasSettingSnapshot` + `AtlasSchemaDescriptor` (+ `AtlasSettingKind` 6-value enum) + `AtlasSearchHit` + `DefaultAtlasProjector` with LWW-by-IssuedAt-then-IssuedBy + composite-key `"<scope>:<path>"`; council pre-merge caught 2 critical bugs (cross-scope path collision + permissive state filter) + 4 amendments; 11 tests. P3b SchemaRegistrationAnalyzer (#513): `Sunfish.Wayfinder.Analyzers.SchemaRegistrationAnalyzer` Roslyn diagnostic `SUNFISH_WAYFINDER001`; new `packages/foundation-wayfinder-analyzers/` package; Directory.Build.props `_SunfishBannedApiTarget` predicate extended to `EndsWith('.Analyzers')` so future analyzer projects auto-inherit the BannedApi-auto-attach exclusion; 4 analyzer tests; perf-tests (P95 ≤ 200ms cold / ≤ 100ms warm at 10K per ADR 0065 council F9) deferred to follow-up. P4 docs + WCAG baseline (#514): `apps/docs/foundation/wayfinder/{overview,wcag,toc}.yml` + foundation toc.yml row; WCAG 2.2 AA + EN 301 549 v3.2.1 baseline; WCAG/a11y subagent council caught 3 WCAG-overclaim issues (SC 4.1.2 + EN 11.7.4 + SC 1.3.1) + 5 mechanical amendments. **48/48 .NET tests + 4 analyzer tests + apps/docs all green.** New packages: `packages/foundation-wayfinder` + `packages/foundation-wayfinder-analyzers`. **Cohort batting average updated:** 22-of-22 substrate amendments needed council fixes (W#42's 4 phases each had council-driven mechanical fixes; this reinforces the pre-merge canonical discipline). **`feedback_council_before_automerge.md` discipline followed for P3a/P3b/P4** (council BEFORE PR creation, not in parallel with auto-merge) — worked cleanly. **Hard prerequisite for** ~ADR 0066 + ~0067 + ~0068 + W#43 (ADR 0009 amendment) — all unblocked.
